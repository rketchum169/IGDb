<% games.each do |game| %> 
    <div class = "game-card">
    <p> 
    <%= game.title %>
    -
    <%= game.rating %>
    -
    <%= link_to("View Info.", game_path(game.id))%>
    </p>
    </div>
<% end %>